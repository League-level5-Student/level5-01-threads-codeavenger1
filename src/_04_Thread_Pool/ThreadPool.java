package _04_Thread_Pool;

public class ThreadPool {

public ThreadPool (Thread t) {
	Thread[] threadArray = new Thread[];
}

	for(

	int i = 0;i<threadArray.length;i++)
	{
		threadArray = new Thread[Worker];
	}

}}
